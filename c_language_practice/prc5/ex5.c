#include <stdio.h>

/*This is a comment. */
int main(int arhc, char *argv[])
{
    int distance = 100;

    //this is also a comment
    printf("you are %d miles aways. \n", distance);

    return 0;
}
